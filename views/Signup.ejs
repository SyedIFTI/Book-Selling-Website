<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
<style>
     .btn {
        transition: background-color 0.3s, transform 0.2s;
        }    
</style>
</head>
<%if(error.length>0){%>
    <div class="absolute top-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-blue-500">
        <span class="inline-block mt-1 mb-1 text-white">
            <%=error%>
        </span>
    </div>
    
    <%}%>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="bg-white shadow-lg rounded-lg overflow-hidden flex w-full max-w-4xl">
        <!-- Image Section -->
        <div class="w-1/2 hidden md:block">
            <img src="/images/signup.jpg" alt="Placeholder Image" class="w-full h-full object-cover">
        </div>
        <!-- Form Section -->
        <div class="w-full md:w-1/2 p-8">
            <h2 class="text-2xl font-bold mb-6">Sign Up</h2>
            <form autocomplete="off" action="/user/register" method="POST" class="space-y-4">
                <div>
                    <label for="fullname" class="block text-gray-700">Full Name</label>
                    <input type="text" id="fullname" name="fullname" class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-zinc-700">
                </div>
                <div>
                    <label for="email" class="block text-gray-700">Email</label>
                    <input type="email" id="email" name="email" class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-zinc-700">
                </div>
                <div>
                    <label for="password" class="block text-gray-700">Password</label>
                    <input type="password" id="password" name="password" class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-zinc-700">
                </div>
                <div>
                    <button class="btn mt-4 text-semibold text-white bg-zinc-900 text-white px-4 py-2 rounded-md shadow-md transform hover:scale-105 hover:border-2 hover:border-zinc-900 w-full hover:bg-white hover:text-zinc-900 hover:font-bold focus:outline-none">
                       SignUp
                    </button>                </div>
            </form>
            <div class="mt-4 text-center
            ">
                <a class="text-zinc-400">have an Account?</a><a href="/login">Login</a>
            </div>
        </div>
    </div>
</body>
</html>